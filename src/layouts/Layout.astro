---
import "@fontsource/changa-one";
import "@fontsource/comfortaa";
import Footer from "../components/Footer.astro";

export interface Props {
  title: string;
}

const { title } = Astro.props;
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <style>
      :root {
        --rainbow-gradient: linear-gradient(
          68deg,
          /* to right, */ #e91e63,
          #f44336,
          #ff5722,
          #ff9800,
          #ffc107,
          /* #ffeb3b, */ /* #cddc39, */ #8bc34a,
          #4caf50,
          #009688,
          #2196f3,
          #3f51b5,
          #8a4af3,
          #673ab7
        );
      }
    </style>
    <meta name="title" content="SuperMassive" />
    <meta
      name="description"
      content="Supermassive is a collective of creatives bringing novel interactive and immersive art experiences to the UK. "
    />
    <meta
      name="keywords"
      content="immersive, art, collective, interactive, uk, attraction, installation"
    />
    <meta name="robots" content="index, follow" />
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <meta name="language" content="English" />
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
  </head>
  <body>
    <slot />
  </body>
</html>
<style is:global lang="scss">
  :root {
    --accent: 124, 58, 237;
    --accent-gradient: linear-gradient(
      45deg,
      rgb(var(--accent)),
      #da62c4 30%,
      white 60%
    );
  }

  html {
    font-family: "Comfortaa", sans-serif;
    background-color: #000;
    color: #fff;
    scroll-behavior: smooth;
  }
  /* a {
		color: #FFF
	} */
</style>
